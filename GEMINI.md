@./AGENTS.md

# Gemini CLI adapter

Use `/memory show` to confirm this file and `AGENTS.md` are loaded. Use `/memory reload` after an
instruction change. Shared project instructions belong in `AGENTS.md`.
